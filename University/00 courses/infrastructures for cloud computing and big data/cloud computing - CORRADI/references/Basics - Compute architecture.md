The way the computation is handled in datacenters is using [[COTS]] machines,but grouped for similarity on hardware.

For example a particular set of machines could have lots of GPUs, so they're leveraged for ML computations, while another particular set of machines could handle lots of disk storage, so they're used for persistent storage.